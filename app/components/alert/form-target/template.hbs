{{searchable-select
    content=targets
    optionGroupPath="type"
    optionValuePath="id"
    optionLabelPath="id"
    prompt="Choose a Target"
    group=model.targetType
    value=model.targetId
}}