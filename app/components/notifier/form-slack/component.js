import Ember from 'ember';

export default Ember.Component.extend({
  // didInsertElement() {
  //   Ember.run.later(() => {
  //     this.$('INPUT[type="text"]')[0].focus();
  //   }, 250);
  // },
});
